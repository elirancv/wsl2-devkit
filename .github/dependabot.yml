# Keep the SHA-pinned GitHub Actions current: Dependabot proposes the new
# commit SHA (with the version comment) via PR, CI validates it, you merge.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: "ci"
