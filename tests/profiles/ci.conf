# Selection profile used by the CI smoke test (stage2-ubuntu.sh --profile).
# Starts from the --yes baseline (Node/Python/Go/CLI on; Rust/Docker/GPG off),
# then these overrides apply.

# GitHub runner images may ship cargo; install Rust so the newrust helper
# check has a consistent outcome either way.
INSTALL_RUST=true
